[package]
name = "bofa-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }

[dev-dependencies]
snapbox = { version = "1.0.0", features = ["cmd"] }

[[bin]]
name = "bofa"
path = "src/main.rs"
